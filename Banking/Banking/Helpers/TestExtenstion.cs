using System;
using System.Collections.Generic;
using System.Collections;
using System.Text;

namespace Banking.Helpers
{
    public static class TestExtenstion
    {
        //public static string ConvertContentToString<T>(this IEnumerable collection, string propertyName)
        //{
        //    collection.
        //    collection = collection.Cast<T>;
        //    string content = "";
        //    for (int i = 0; i < collection.Count(); i++)
        //    {
        //        if (typeof(Enumerable).IsAssignableFrom(collection.ElementAt(i).GetType()))
        //            content += ConvertContentToString((IEnumerable<T>)collection.ElementAt(i)
        //                , collection.ElementAt(i).GetType().Name);
        //        else
        //            content += collection.ElementAt(i);
        //        if (i != collection.Count() - 1)
        //            content += ",";
        //    }
        //    string result = $"{propertyName}[{content}]";
        //    return result;
        //}
    }
}
